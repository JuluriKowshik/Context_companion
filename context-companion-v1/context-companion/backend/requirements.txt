# Minimum versions, not exact pins. pydantic>=2.12 is the first release with
# prebuilt wheels for Python 3.14; older pins force a Rust compile from source.
fastapi>=0.118
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.12
pydantic-settings>=2.10

# Local lexical difficulty scoring for the dictionary-first classifier.
wordfreq>=3.1
nltk>=3.10
